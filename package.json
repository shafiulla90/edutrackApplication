{
  "name": "edutrack-monorepo-root",
  "version": "1.0.6",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && next build",
    "dev": "cd frontend && next dev"
  },
  "dependencies": {
    "next": "^14.1.0"
  }
}
